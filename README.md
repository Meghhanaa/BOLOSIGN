# Signature Injection Engine

This project demonstrates a full-stack prototype for placing and burning signatures
onto PDFs with correct coordinate translation between browser and PDF space.

## Tech Stack
Frontend: React + react-pdf + react-rnd  
Backend: Node.js + Express + pdf-lib  
Database: MongoDB  
Security: SHA-256 hashing  

## Run with Docker
docker-compose up --build
